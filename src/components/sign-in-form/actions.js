import signInAction from '../../store/Authorization/actions/signInAction';
import signInButtonAction from '../../store/Authorization/actions/signInButtonAction';

const actions = {
  signInAction,
  signInButtonAction,
};

export default actions;
